import { styled } from '@material-ui/core/styles';

export const CreditCards = styled('section')({
  marginTop: '24px',
  marginBottom: '32px',

  h3: {
    marginBottom: '16px',
  },

  p: {
    marginBottom: '24px',
  },
});
